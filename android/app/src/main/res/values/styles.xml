<?xml version="1.0" encoding="utf-8"?>
<!--
Tujuan: Theme Android SmartPatrol termasuk splash dark navy saat boot.
Caller: AndroidManifest MainActivity dan Android SplashScreen API.
Dependensi: Resource warna app_boot_background dan icon foreground SmartPatrol.
Main Functions: AppTheme, AppTheme.NoActionBar, AppTheme.NoActionBarLaunch.
Side Effects: Mengatur warna boot, splash icon, dan background awal WebView.
-->
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/app_boot_background</item>
        <item name="android:colorAccent">#22D3EE</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/app_boot_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <item name="android:windowBackground">@color/app_boot_background</item>
    </style>
</resources>
